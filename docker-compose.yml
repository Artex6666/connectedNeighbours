services:
  front:
    build:
      context: .
      dockerfile: front/Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped

  # Placeholders pour la suite (API Node, MongoDB, Neo4j, etc.)
  # api:
  #   build: ./api
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - mongo
  # mongo:
  #   image: mongo:7
  #   volumes:
  #     - mongo_data:/data/db

# volumes:
#   mongo_data:
